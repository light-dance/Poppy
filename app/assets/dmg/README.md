# DMG Assets

Place the Finder window background image here:

```text
background.png
background@2x.png
```

Expected sizes:

```text
background.png    680 x 457
background@2x.png 1360 x 914
```

The DMG script uses the exported app's `AppIcon.icns` as the mounted disk icon.
