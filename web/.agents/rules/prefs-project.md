Use bun instead of npm or pnpm
Prefer bun's built in tooling over packages (postgres, redis, sql integrations)
When adding packages, use `bun add <package-name>` over trying to add the package to package.json
