[install]
# Require package versions to be at least 7 days old before new resolution
minimumReleaseAge = 604800

[install.security]
# Use socket to scan instalations
scanner = "@socketsecurity/bun-security-scanner"
